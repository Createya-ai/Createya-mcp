# Codex CLI MCP config
# Path: ~/.codex/config.toml
# After editing — перезапусти codex.

[mcp_servers.createya]
url = "https://api.createya.ai/mcp"
http_headers = { Authorization = "Bearer crya_sk_REPLACE_WITH_YOUR_KEY" }
enabled = true
